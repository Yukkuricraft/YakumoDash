import { FileModeToStringPipe } from './file-mode-to-string.pipe';

describe('FileModeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FileModeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
