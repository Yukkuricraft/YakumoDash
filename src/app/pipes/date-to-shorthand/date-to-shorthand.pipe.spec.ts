import { DateToShorthandPipe } from './date-to-shorthand.pipe';

describe('DateToShorthandPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToShorthandPipe();
    expect(pipe).toBeTruthy();
  });
});
